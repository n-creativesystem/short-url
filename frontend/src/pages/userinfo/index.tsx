import UserInfoContainer from '@/components/pages/userinfo';
import { FC } from 'react';

const Page: FC = () => {
  return <UserInfoContainer />;
};

Page.displayName = 'UserInfoPage';

export default Page;
